# Compiled output and build artifacts
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties

# Maven Build Process State file & Wrapper JAR checksums
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar.md5
.mvn/wrapper/maven-wrapper.jar.sha1
# Keep the wrapper jar definition itself (.mvn/wrapper/maven-wrapper.properties)
# Keep the wrapper script itself (.mvn/wrapper/mvnw)

# Application Configuration Files (Ignored as requested + profiles/secrets)
# !!! Ensure you have alternative ways to provide required config (env vars, etc.) !!!
application.properties
application.yml
application-*.properties
application-*.yml
secrets.properties
secrets.yml
*.env
.env*

# Log files
*.log
logs/

# IDE specific files
# IntelliJ
.idea/
*.iml
*.ipr
*.iws
# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace
.history/
# Eclipse
.metadata/
bin/
tmp/
*.tmp
*.bak
*.swp
*~.nib
local.properties
.settings/
.loadpath
.recommenders
.externalToolBuilders/
.deployables/
.cproject
.project
.classpath
.factorypath
# STS (Spring Tool Suite)
.sts4-cache/
# NetBeans
nbproject/
build/
nbbuild/
dist/
nbdist/
.nb-gradle/

# Java / JVM Crash logs
hs_err_pid*
replay_pid*

# OS generated files
# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
[Dd]esktop.ini
$RECYCLE.BIN/
*.lnk
# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon*
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk
# Linux
*~
.directory
core

# Other package files (less common to commit)
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

### Node / React Frontend ###

# Dependency directories (MUST be ignored!)
node_modules/
jspm_packages/

# Build output folders (Common names)
build/
dist/
out/
.output/
.next/ # Next.js build output
.vercel/

# Log files
npm-debug.log*
yarn-error.log
yarn-debug.log*
pnpm-debug.log*

# Environment variables files (NEVER commit secrets!)
# Commit a .env.example file instead to show required variables.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.development
.env.production
.env.*.local

# Coverage directory from test runners
coverage/
.nyc_output/

# Optional tool directories / cache files
.eslintcache
*.sqlite # Sometimes used by tools
.cache # Common cache dir name (e.g., Remix, Gatsby)
.turbo # Turborepo cache

# Optional TypeScript build info cache
*.tsbuildinfo

# Optional framework specific folders
.svelte-kit/ # SvelteKit
# public/build/ # Remix build output in public
# .nuxt/ # Nuxt build output

# Optional Cypress test output folders
cypress/videos/
cypress/screenshots/
cypress/downloads/
cypress/fixtures/example.json